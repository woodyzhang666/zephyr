
DT_COMPAT_WCH_UART := wch,wch-uart

config UART_WCH
	bool "WCH UART"
	default $(dt_compat_enabled,$(DT_COMPAT_WCH_UART))
	select SERIAL_HAS_DRIVER
	select SERIAL_SUPPORT_INTERRUPT
	help
	  This option enables the UART driver for many wch SOCs.
